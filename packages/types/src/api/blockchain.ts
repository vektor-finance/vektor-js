import { NetworkMode } from './network'

export interface BlockchainsListRequestParams {
  networkMode?: NetworkMode
}

export interface BlockchainsSymbolsRequestParams {
  networkMode?: NetworkMode
}
